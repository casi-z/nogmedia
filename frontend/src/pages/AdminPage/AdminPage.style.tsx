import {
    Box,
    useTheme

} from "@mui/material"
import styled from 'styled-components'

function useAdminPageStyles() {

    const theme = useTheme()


   return (
       styled.div`
           
       `
   )
}
export default useAdminPageStyles