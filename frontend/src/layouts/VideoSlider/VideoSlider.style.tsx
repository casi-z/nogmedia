import {
    Box,
    useTheme

} from "@mui/material"
import styled from 'styled-components'

function useVideoSliderStyles() {

    const theme = useTheme()


   return (
       styled.div`
           
       `
   )
}
export default useVideoSliderStyles