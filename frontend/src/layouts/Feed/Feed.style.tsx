import {
    Box,
    useTheme

} from "@mui/material"
import styled from 'styled-components'

function useFeedStyles() {

    const theme = useTheme()


   return (
       styled.div`
           
       `
   )
}
export default useFeedStyles